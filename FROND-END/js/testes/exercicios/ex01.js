let velocidade = 70;

if (velocidade > 80) {
  console.log(`Velocidade ${velocidade} acima do permitido`);
} else if (velocidade < 80) {
  console.log(`Velocidade ${velocidade} abaixo do permitido`);
} else {
  console.log(`Velocidade ${velocidade} no limite permitido`);
}
